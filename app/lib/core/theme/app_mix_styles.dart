import 'package:flutter/material.dart';
import 'package:mix/mix.dart';

import 'app_colors.dart';

final loveCardStyle = BoxStyler()
    .color(Colors.white.withValues(alpha: .90))
    .paddingAll(32)
    .borderRounded(16);

final loveCardBorder = Border.all(color: border);
