[build]
dockerfilePath = "Dockerfile.backend"
watchPatterns = ["backend/**", "app/**", "Dockerfile.backend", "railway.toml"]

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ALWAYS"
restartPolicyMaxRetries = 10
numReplicas = 1
