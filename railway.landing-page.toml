[build]
dockerfilePath = "Dockerfile.landing-page"
watchPatterns = ["landing-page/**", "Dockerfile.landing-page", "railway.landing-page.toml"]

[deploy]
healthcheckPath = "/pt"
healthcheckTimeout = 300
restartPolicyType = "ALWAYS"
restartPolicyMaxRetries = 10
numReplicas = 1
